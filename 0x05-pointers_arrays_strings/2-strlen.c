#include <stdio.h>

/**
 *int _strlen - returns the length of a string
 *
 *function swap_int(): Swaps the two integers function
 *
 *Return: 0
*/
int _strlen(char *s){

char a[100];

int length;

  length = strlen(a);

return 0;

}
